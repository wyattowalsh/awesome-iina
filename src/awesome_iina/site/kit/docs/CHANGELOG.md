# Changelog

## 2.1.1 — final audit maintenance patch

- F01: configure stable origin-relative manifest identity in source and every integration path; test root, nested, multi-project and relocation behavior.
- F02: use effective CSS kerning in all editable SVG exchange files; verify exact-font glyph positions against unchanged production outlines.
- F03: canonical wordmark in the responsive demo; shared primary/optional allowlist for installation and production ZIP. Historical hero retained unchanged and opt-in.
- Clarify F04–F06 context, platform evidence and owner-permission boundaries without pretending to perform user research or grant rights.
- Add focused regression/renderer checks, verify real v2.1.0 upgrade/undo, and bind package preservation/rebuild/extraction results.
- No production-artwork redesign, no font redistribution, no remote publication.

## Earlier release history

# Change log

## 2.1.0 — integration and verification continuation

Preserves the 2.0.0-review.1 visual identity. All 35 delivery artwork files remain
byte-identical, including after rebuilding with the recorded local fonts. No new
concept was generated or treated as approved.

Added standard-library plan/apply/check/undo/recover integration; source- and
target-hash guards; per-file backups and modes; marker-aware README preservation;
explicit template/public-directory support; custom site URL and social-size
configuration; portable build root; font-hash gating; import-time mutation removal;
hardened full/production-only packaging; automated regression tests; and new local
verification. Historical validation is retained separately under provenance/.

The full kit still contains source artwork and earlier audit context. The production
ZIP contains only delivery assets, snippets and integrity metadata. No font binaries,
GitHub writes, owner approval or deployed browser acceptance are implied.

---

# Changes from the audited kit

## 2.0.0-review.1 — 2026-09-16

- Selected a compact indexed-media mark with one offset tab; removed tiny controls,
  puzzle shape, extra panes and sparkle from the primary identity.
- Added deliberately distinct 16/24 optical geometry, multi-size PNG and exact-frame ICO.
- Added real path-based SVG and true transparent dark/light/monochrome variants.
- Rebuilt social type in editable layers with a short two-line role descriptor.
  Retained waves are secondary texture; no alternative player logo competes in the card.
- Kept the original README hero export byte-for-byte for optional supplementary use.
  Supplied live name, scope and independence copy and a working local integration proof.
- Completed all basic Open Graph fields, declared actual dimensions, and corrected
  intended base paths separately from localhost demo paths.
- Recorded font dependency/usage boundaries, proposed palette roles and explicit sRGB
  output interpretation. Original untagged/credential-bearing files remain unchanged.
- Split metadata preparation from deployment verification and corrected parallel task
  dependencies. Added a whole-system coherence check rather than approving isolated files.
- Kept platform/human review conditions explicit; no replacement numeric score, native
  app compatibility, public deployment, user study or legal approval was manufactured.
