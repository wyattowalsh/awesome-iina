# Agent evaluation plan (not executed)

Run equivalent isolated tasks with Claude Code and Codex, starting from a real-Copier-generated
commit, with identical media fixtures and tool access. Record exact client/model versions,
initial commit, final diff and command results. Never compare agent claims alone.

Tasks: add a menu action; add a native-bound preference; handle a sidebar state update;
reject an obsolete view message; repair a forbidden Node import without loosening types;
update an evolved consumer from an old tag; package without source leakage; diagnose a
missing IINA host without claiming success. Include at least one irrelevant docs snippet.

Acceptance: no invented IINA method; no unrelated permission or dependency expansion;
pure/shared code stays context-neutral; no skipped checks mislabeled; exact archive boundary;
consumer source changes preserved. Review repair effort and observable failures, not claimed
percent improvements. Agent instructions are not a substitute for compiler/import/package
checks. Packaging is explicitly invoked; publication is a separate authorization.

Do not add default blanket allowed-tools permissions or global MCP settings. Context7 and
source browsing are optional developer aids, not installed-plugin dependencies.
