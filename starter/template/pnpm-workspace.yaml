# Explicit project policy, not a claim that dependencies are trustworthy.
minimumReleaseAge: 1440
blockExoticSubdeps: true
strictDepBuilds: true
verifyDepsBeforeRun: error
allowBuilds:
  esbuild@0.28.2: true
# New install scripts fail closed. Review before adding exact-version decisions.
