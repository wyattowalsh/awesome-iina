# Optional provisioning only. Commands live in package.json and justfile.
# Run mise install explicitly. Generate mise.lock with mise lock after review.
[tools]
node = "24.21.0"
pnpm = "12.4.1"
python = "3.13.15"
just = "1.58.0"
uv = "0.12.13"
